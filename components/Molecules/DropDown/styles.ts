import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dropdown: {
    height: 50,
    backgroundColor: "white",
    borderColor: "gray",
    borderWidth: 0.5,
    borderRadius: 8,
    paddingHorizontal: 8,
    width: 130,
  },

  placeholderStyle: {
    fontSize: 16,
  },
  selectedTextStyle: {
    fontSize: 16,
  },
});
